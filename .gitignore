# Binaries for programs
main

# Kubernetes secrets (override with real values locally)
**/secret.yaml
**/*-secret.yaml

# Terraform/OpenTofu
*.tfstate
*.tfstate.*
.terraform/
infrastructure/.terraform/
.terraform.lock.hcl

# Kubeconfig
kubeconfig.yaml
*.kubeconfig

# SOPS (future)
*.sops.yaml
