apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ollama-edge-router
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
  - host: ollama.cluster.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ollama-service
            port:
              number: 80
